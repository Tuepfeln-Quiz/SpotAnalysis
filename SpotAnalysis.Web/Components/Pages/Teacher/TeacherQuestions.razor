@page "/teacher/questions"

<div class="card card-body h-30 mb-2 border border-2">
    <form class="row" method="get" asp-page="/Search">
        <div class="search-container col-12">
            <div class="row">
                @*                     <button class="btn btn-primary col-2">Add</button>
 *@                    <input type="text"
                                             asp-for="SearchQuery"
                                             placeholder="Search..."
                                             class="search-input col-8" />
                <button type="submit" class="search-btn col-2">&#128269</button>
            </div>
        </div>
    </form>
    <table class="table table-dark table-striped table-hover">
        <thead>
            <tr>
                <th>Students</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                @*Aus Gruppe selctierte students*@
                @foreach (var a in teacherQuestions)
                {
                    <td class="text-center">@a.Name</td>
@*                                             <td>@a.Roles</td>
                        <td>
                            <button class="btn btn-secondary">Edit</button>
                            <button class="btn btn-danger">Remove</button>
                        </td>
 *@                        <td class="btn btn-danger">
                        Delete
                        <i class="bi bi-trash3"></i>
                    </td>
                } 
            </tr>
        </tbody>
    </table>





</div>

@code {

    List<ChemicalViewModel> teacherQuestions = new List<ChemicalViewModel>();
}
