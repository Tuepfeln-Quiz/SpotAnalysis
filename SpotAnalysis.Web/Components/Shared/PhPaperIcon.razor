<svg xmlns="http://www.w3.org/2000/svg" width="@Size" height="@Size" viewBox="0 0 16 16" fill="currentColor" style="vertical-align: -2px;">
    <!-- Linke Hälfte des Streifens (heller) -->
    <polygon points="0,2 7.5,2 7.5,14 0,14 2,12 0,10 2,8 0,6 2,4" fill="currentColor" opacity="0.55"/>
    <!-- Rechte Hälfte des Streifens (dunkler) -->
    <polygon points="8.5,2 16,2 14,4 16,6 14,8 16,10 14,12 16,14 8.5,14" fill="currentColor" opacity="0.85"/>
    <!-- Mittlerer Streifen (Trennlinie) -->
    <rect x="7" y="1.5" width="2" height="13" fill="currentColor" opacity="0.3"/>
</svg>

@code {
    [Parameter] public int Size { get; set; } = 14;
}
